import styled from 'styled-components';

export const Submit = styled('button')({
    color: '#222',
    border: '1px solid #6f6f6f',
    borderRadius: '4px',
    padding: '8px 16px',
    cursor: 'pointer'
});